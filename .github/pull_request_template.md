## Summary

<!-- What does this change do, and why? -->

## Changes

<!-- Bullet the notable changes. -->
-

## Checklist

- [ ] `poetry run pytest -m "not integration"` passes (100% coverage)
- [ ] `poetry run ruff check . && poetry run ruff format --check .` clean
- [ ] `poetry run mypy src` clean
- [ ] Docs updated if behavior or the public API changed (`README.md` / `design.md`)
